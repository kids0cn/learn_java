package OOP.有参构造;

public class testApp {
    public static void main(String[] args) {
        // Student s = new Student("传参啦"); 调用有参构造就会默认覆盖无参构造
        Student s = new Student(); // 调用无参构造
    }
}
